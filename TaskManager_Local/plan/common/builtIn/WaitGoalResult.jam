
PLAN PERFORM WaitGoalResult($goalID){
	ID: "http://www.arbi.com//WaitGoalReport"
	PRECONDITION:
		FACT GLUtility($glUtility);
		FACT GoalResult($executor, $goalID, $result);
	BODY:
		RETRACT GoalReport($a, $goalID, $c);
	UTILITY : 10;
}