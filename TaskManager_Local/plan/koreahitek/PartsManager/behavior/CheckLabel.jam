PLAN PERFORM CheckLabel() {
	ID: "http://www.arbi.com//CheckLabel"
	PRECONDITION:
		//FACT robotStatus($robot, $status);
		//$status == "Ready";
		FACT ExtraUtility($helper);
		FACT isro:agent($agent);
	BODY:
		PERFORM isro:startLog("PERFORM", "CheckLabel");
		
		$actionID = $helper.getActionID($agent);
		$data = "(CheckLabel \"" + $actionID + "\")";
		PERFORM isro:RequestBehavior($actionID, $data);
	
		PERFORM isro:endLog("PERFORM", "CheckLabel");
	UTILITY: 1; 
	
}